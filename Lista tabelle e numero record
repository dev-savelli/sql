
--ESTRAE TUTTE LE TABELLE CON IL NUMERO DI RECORD

SELECT DISTINCT 'SELECT * FROM ' +       NAME tabella, NAME,
       ROWS righe
FROM SYS.TABLES T
     INNER JOIN SYS.PARTITIONS P ON T.OBJECT_ID = P.OBJECT_ID
WHERE rows > 0
ORDER BY righe DESC;
